
1. Untuk buat Telegram Bot yang boleh **membaca arahan (`/ups state`)** dan **membalas (reply)**, kita perlukan kaedah yang dipanggil _Long Polling_. Install JQ untuk baca format JSON dari Telegram.

```bash
sudo apt install jq
```